$( document ).ready(function() {
    //console.log( "ready!" );
    // $('input[type="file"]#imgEdit').change(function(e){
             // var fileName = e.target.files[0].name;
             // var fullPath = 'images/' + fileName
             // $('#imgShow').attr("src", fullPath);
            // alert('The file "' + fullPath +  '" has been selected.');
         // });
});
